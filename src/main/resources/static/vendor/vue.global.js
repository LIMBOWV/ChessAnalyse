// Placeholder for Vue 3 (local copy for static analysis)
// In production you can replace this with a full Vue build or use CDN.
// The actual project uses CDN in runtime; this file prevents IDE warnings.

/* Minimal stub to avoid parser errors when IDE checks for local files. */
(function(){
  // noop
})();

